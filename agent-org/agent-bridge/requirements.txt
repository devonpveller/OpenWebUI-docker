# agent-bridge runtime dependencies (PLAN §5.1 / OD-2: Python FastAPI + Pydantic + Instructor).
# Pinned loosely (compatible-release) — verify current licenses at adopt time (TOOLING §6).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
SQLAlchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0            # Postgres driver (production state store — fail-safe persistence)
aiosqlite>=0.20,<1.0         # SQLite driver (default/dev + deterministic gate-FSM tests)
httpx>=0.27,<1.0             # REST poster, model calls, Open Brain mirror
websockets>=12,<14           # Mattermost WebSocket event bus consumer
openai>=1.30,<2.0            # OpenAI-compatible client -> llm-gateway (llama-cpp) / llm-gateway-cloud
instructor>=1.3,<2.0         # schema-validated structured output (small-model reliability, TOOLING §3.2)
tenacity>=8.2,<10.0          # bounded retry/backoff (429 tolerance, event redelivery)
