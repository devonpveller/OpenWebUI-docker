# agent-org — local/dev artifacts (never commit)
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/

# Local state stores (SQLite dev DB) + secrets
*.db
*.sqlite3
docker/.env
.env

# Bridge default SQLite file
agent-bridge/agent_bridge.db

# P0.5 capability-floor result artifacts (the decision is recorded in IMPLEMENTATION-NOTES)
p0_5*.json
agent-bridge/p0_5*.json
