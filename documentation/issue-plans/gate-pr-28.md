## Verdict: RECOMMEND-MERGE

## Rubric
- Solves the issue INTENT (not merely tests-green): **pass** — the reporter's exact message can no longer yield `ry>`; every tag/partial-word path now falls through to the domain default, which is precisely the expected behavior the issue states.
- Evidence quality (failing→passing repro shown): **pass** — RED (8/14 at tests-only `21750fd`) and GREEN logs are real and reproduce; the PR description is one commit stale (head `b989b4a` has 21 tests, evidence 12/21→21/21 lives in that commit's message), independently re-verified below.
- Scope discipline (no drive-by changes): **pass** — 3 files, all `smolcrawl/`; the one behavior change beyond the minimal fix (suffix-pattern-first ordering) is explicitly ordered by the audited plan (Approach step 2).
- SERVICE-LIFECYCLE compliance (if service-shaped): **n/a** — no service surfaces touched, correctly: the `smolcrawl-pipelines` image was retired in `a18aa0c`, so compose/recovery/watchdog/backup edits would themselves have been drive-bys.
- Security (secrets, gateway-only routing, branch policy): **pass** — no secrets or endpoints in the diff; no inference-routing surface; branch cut from `9e46575` on `development` and PR targets `development` per branch policy (`main` untouched).

## Reasoning

**The handed diff is the real PR head.** `git diff 9e46575 b989b4a` reproduces the supplied diff byte-for-byte (pipeline blob `a23e5e1..c09b8c2`, test blob `052c178`, 3 files). Commit chain is tests-first: `21750fd` (tests only, 14 tests) → `feb2457` (first fix) → `b989b4a` (hardening + 7 tests, 21 total), all on `origin/issue/17-kb-name-extraction`, merge-base with `origin/development` = the plan's pinned base `9e46575`. `origin/development` has advanced only by `207d1ed` (CI, non-smolcrawl), so the merge is clean.

**Evidence verification.** Python execution is permission-gated in this review session, so I verified by deterministic hand-trace of the regex semantics against all 21 head tests — the same method the audited plan used, and this regex is simple enough for the trace to be exact:

- At base (`smolcrawl_pipeline.py:815`, unbounded `into|to|as` + `(.+?)` capture): exactly 12 of 21 tests fail — the reporter repro (`manda[to]ry>` → `ry>`), 3 mid-word captures, 4 word-initial captures (`today`→`day`, `asap`→`ap`, `tomorrow`→`morrow`, `Assistant:`→`sistant: proceed`), suffix leak, 81-char capture, and both `_normalize_kb_name` bracket cases. That matches `b989b4a`'s claimed "RED at base = 12/21", and the 8 of those present in the 14-test suite at `21750fd` match the PR description's RED log line-for-line.
- At head: all 21 pass by trace. Key checks: `\b(?:into|to|as)\b\s+` (`smolcrawl_pipeline.py:824-825`) kills both mid-word and word-initial hits; `[^<>\n\r]{1,80}?` makes `</query> <mandatory>` and `</instructions>` tails *fail the match* so control reaches the `SmolCrawl - {netloc}` fallback at `:832`; the capture class deliberately keeps `'` so `into Bill's Docs` still works (the plan's stated correction to the issue's suggested class); both helpers are `@staticmethod`, so the tests' unbound calls are valid; the pydantic stub makes the suite stdlib-only as claimed.

**The org self-corrected mid-flight, honestly.** `feb2457`'s shared leading `\b` left word-initial hits open (`today` → `day`); `b989b4a` fixed it per-alternative and added the `{word-start, mid-word, whole-word} × {match, no-match}` matrix. The follow-up's commit message documents the hole and the updated evidence rather than papering over it. The only blemish is that the PR *description* was never refreshed — its logs cite `feb2457` and "Ran 14 tests" while the head is `b989b4a` with 21. The evidence chain is complete in the commits, so this is merge-hygiene, not a credibility gap; the human merger should read `b989b4a`'s message alongside the description (or ask the org to refresh the description first — non-blocking).

**Accepted trade-offs, both plan-sanctioned or cosmetic:** (1) reviving the suffix pattern means names legitimately containing ` with/using/from ` now truncate (`into Cooking with Gas` → `Cooking`) — the plan explicitly ordered the suffix variant first; (2) the suffix terminator lacks the trailing `\b` the issue's sketch had, so `into My KB withdrawal` truncates at the embedded `with` — an edge case in dormant code that cannot reintroduce the reported bug class, since `<`/`>` remain uncapturable and normalization now strips dangling brackets (`:60`).

This is dormant source (zero live risk, per the operator-elected Option 2), the fix matches the audited plan step-for-step, and the test suite pins the whole failure family, not just the reported symptom.
