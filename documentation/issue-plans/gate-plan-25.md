## Plan verdict: GO

## Rubric
- Grounded (every cited path/line exists at the pinned base; claims re-derived, not trusted): **pass** — all ~15 citations re-derived exactly at `a1322f5` (which is on `origin/development`); not one line number was off.
- Dispatchable scope (bounded for a small local model; triage honest; one issue, no drive-bys): **pass** — one new host script + fixtures + runbook + a CLEANUP-PLAN tick; both adjacent temptations (OB1 callback lane, ON-audio-failure alert) explicitly filed as follow-ups, not absorbed.
- Validation is real (RED-at-base repro named; exact commands; T2/live steps assigned to the HOST harness, never the sandboxed worker): **pass** — RED/logic/GREEN all name exact commands; the worker's loop is confined to sandbox-executable JSON validation, with an explicit prohibition on `powershell`/`docker exec`/Mattermost and on claiming host tiers.
- Live-surface honesty (touches_live and bind-mount writes declared; OB1-submodule discipline if wiki/OB1 paths): **pass** — `touches_live: false` is accurate (no container/bind-mount change; state under gitignored `logs/`); live GPU load during GREEN and the scheduled-task install are declared as operator-gated interlocks; the OB1 lane is correctly deferred to a submodule PR + gitlink bump.
- Security screen (plan directs no secret movement, no gateway bypass, no branch-policy violation, no unrelated file contact): **pass** — token stays runtime-read from the gitignored env file (the exact `notify-mattermost.sh:14,42-51` pattern); the read path is the documented GET-only operator lane, not an inference bypass; the routing guard's `$badPattern` only fires on `*-upstream` base/host assignments, which this plan never makes.

## Reasoning

Every load-bearing claim survived re-derivation at the pinned base:

- **Silent-wait problem is real.** `llm-queue/src/llm_queue/main.py:47` describes the service as "hold-and-dispatch"; `scheduler.py:129-178` heaps the waiter and `await_dispatch` (`scheduler.py:180-185`) blocks on `dispatched_event`. The only side-channel is `EventSink`, documented best-effort at `events.py:9-11` with the never-break-inference write at `events.py:69-87`. No notification path exists.
- **The data the notifier needs already exists.** `snapshot()` (`scheduler.py:272-303`) carries per-waiting-row `key`/`waited_s`/`est_wait_s` and per-model `avg_T_s`; `/observe/queue/estimate` is real (`routes/observe.py:34-38` → `routes/control.py:60-79`) and the `/observe/*` namespace is GET-only by design (`observe.py:1-13`), bridged via `config/litellm.config.yaml:114-120`.
- **Host-side posture is correctly derived.** `inference/docker-compose.yml:248-256` confirms no host port and `docker exec` as the operator lane; line 212 confirms curl exists in the llm-queue image; `control.py:8-11` states the mutating-verb invariant the plan promises never to touch. The pre-commit routing guard (`scripts/checks/check-llm-gateway-routing.ps1:49`) flags only `*-upstream` base/host assignments — the new script's `docker exec llm-queue … /observe/queue` cannot trip it, and the control.py docstring explicitly sanctions direct operator `docker exec` use of the queue.
- **Bookkeeping claims hold.** The K.10 PLANNED item is at base `CLEANUP-PLAN.md:1128-1132` and the podcast/research routing notes at 1103-1112 — both sit before the only working-tree drift hunks (base lines 1155/1328), so base numbering is intact. `callback_armed` fires only at submission hand-off (`owui/tools/deep_research.py:155-161`) — no interim signal, as claimed. `logs/` is gitignored (`.gitignore:15`); `priority_demo.py` and `SERVICE-LIFECYCLE.md` exist at base; neither `queue-eta-notify.ps1` nor the runbook exists yet (no collision). The plan even honestly flags that the existing watchdog's ValidateSet is `check|daemon|install-service` (`stack-watchdog.ps1:7`) versus its own proposed `install-task`.
- **The evidence assignment matches the two-gate/two-harness law** (worker = files only; HOST harness = RED/logic/GREEN + ps1 hooks), which is exactly the M.7/M.8 split the operator codified.

Two caveats for the PR gate, neither gate-failing:

1. **Frontmatter `touched_paths` omits the fixtures** (`scripts/checks/fixtures/queue-eta-*.json`) that the Validation section makes worker deliverables. Metadata drift only — radar/status tooling will under-report the diff; the PR gate should expect those files.
2. **Fixture shape is specified as the per-model `snapshot()` object** (top-level `waiting`, per the sandbox assert), but the live `GET /observe/queue` the script polls returns the wrapped board `{"models": {name: snapshot}, "held_total", …}` (`control.py:31-38`). If `-SnapshotFile` is to substitute for the live exec, the fixture should be board-shaped (or the parser must accept both). The host logic tier would catch a mismatch immediately and the fix is one file — worth the dispatcher noting in the worker's brief, not a re-plan.
