## Verdict: DENY

## Rubric
- Solves the issue INTENT (not merely tests-green): **pass** — the head code (`8234db9`) implements the plan's MM lane faithfully: wrapped-board parser matching `llm-queue/src/llm_queue/routes/control.py:30-38`, per-(key, model) aggregation, threshold/cooldown/dedup, fail-soft posting; the OWUI lane is the plan's explicit follow-up.
- Evidence quality (failing→passing repro shown): **fail** — the PR description's only worker-owned evidence block is attempt-1 output that cannot be produced by the committed files; its claim "run in this workspace against the committed fixtures" is false.
- Scope discipline (no drive-by changes): **pass** — `git diff origin/development...8234db9` is exactly the 5 planned files (fixtures anticipated by gate-plan-25 caveat 1); one wording nit: the CLEANUP-PLAN tick strikes the whole "MM/OWUI" bullet as DONE though only the MM half shipped.
- SERVICE-LIFECYCLE compliance (if service-shaped): **n/a** — no container added/moved; the new scheduled task and its uninstall are covered in the runbook, which is the right surface for a host-side check script.
- Security (secrets, gateway-only routing, branch policy): **pass** — token runtime-read from gitignored `agent-org/docker/.env` exactly per `scripts/notify-mattermost.sh:42`; probe path is the GET-only `/observe/queue` operator lane sanctioned by `control.py:1-11`; base is `development`; no secrets in the diff (channel id is not a secret — it's already committed in `notify-mattermost.sh:19`).

## Reasoning

**The code fixed what attempt 1 was denied for; the evidence was carried forward from attempt 1 unregenerated.** Branch forensics prove this directly:

1. **The committed fixtures are the wrapped board shape** — top-level keys `models`/`held_total`/`max_total_connections` (`scripts/checks/fixtures/queue-eta-busy.json`), matching the real route (`control.py:34-38`), and `Get-FlaggedLanes` now iterates `$Snapshot.models` (`queue-eta-notify.ps1`, the block commented "waiting/avg_T_s do NOT exist at the top level"). The runbook's validation block asserts exactly this shape. This is the captured-contract fix the steer demanded — commit `3f013b3` ("consume the real wrapped /observe/queue payload") did it deliberately.
2. **But the PR description's "Sandbox evidence" transcript is from the DENIED attempt-1 tree.** Its asserts read `busy["waiting"]`, `busy["permits_free"]`, `busy["model"]` at the **top level** — against the committed fixtures the very first assert (`missing top-level keys`) raises, so "FIXTURE ASSERTS: all passed" is impossible at head. Conclusively: the transcript's flagged lanes name model `llama3.1-8b-instruct`, a string that appears **nowhere** in the diff — it exists only in the attempt-1 fixture (`git show c82e3b4:scripts/checks/fixtures/queue-eta-busy.json`, `"model": "llama3.1-8b-instruct"`). The description also still says `-Mode check|daemon|install-service` while head and runbook say `install-task`. The evidence surface this gate is told to verify was not regenerated after the steer, and it affirmatively claims currency it doesn't have — the same false-VERIFIED class the org's red-green law exists to kill.
3. **Real verification exists, but in the wrong place and wrong shell.** Commit `3f013b3`'s message documents an actual pwsh 7 + python3 run of the corrected code (2 lanes post, passes 2-3 suppressed, idle clears state, ISO-8601 state survives). That is genuinely encouraging — and it is (a) absent from the PR description, (b) pwsh 7 on Linux, not the Windows PowerShell 5.1 this script targets, and (c) a deviation from the evidence assignment, which prohibited the sandbox from attempting `powershell` and from claiming the logic tier. The host-harness tiers (RED/logic/GREEN on PS 5.1) have not been run; I attempted the logic tier from this gate session and was blocked by execution approvals, so my review is inspectional + git-forensic, not executional — one more reason the description's unreproducible transcript cannot stand in as the merge evidence.
4. **One high-confidence functional defect for the host tier to confirm:** `Install-ScheduledTask` embeds `$($MyInvocation.MyCommand.Path)` in the task action — inside a function that resolves to `$null`, so the task would register with `-File ""` and the daemon would silently never start (a silent-failure mode this stack has been burned by before). The correct token is `$PSCommandPath`. This idiom was copied from `stack-watchdog.ps1:1573`, so it's an inherited trap, not worker carelessness — but it ships broken in 1 of the 3 modes and the fixture tiers can't catch it.

Verified independently: fixture threshold math is right (w1 = 135.7s, w2 = 155.0s ≥ 120; w3 = 15s under → exactly the two claimed lanes), the runbook's python block does pass against the committed fixtures by inspection, and the file list contains no drive-bys.

## If DENY: orchestration adjustment plan

This does **not** need a code re-plan — the planning contract landed. Adjust the dispatch pipeline's PR-preparation and steer-completion steps:

1. **Evidence-freshness invariant (prepare-PR stage):** the PR description's evidence block must be **regenerated from the final head tree** on every push — re-run the asserts, paste the new output, delete the old block. Extend the existing red-green fail-closed principle to evidence transcripts: a transcript is only evidence if reproduced at HEAD. Concretely enforceable by machine: reject a PR body whose quoted evidence contains tokens (model names, mode names, key names) that grep to zero hits in the head diff — `llama3.1-8b-instruct` and `install-service` would both have tripped it here.
2. **Steer-completion diff check:** when a gate deny names contract items, attempt N+1's PR body must be diffed against attempt N's before dispatching to the gate; an unchanged evidence block is an automatic bounce back to the worker with the instruction "regenerate evidence at head", not a gate cycle.
3. **Evidence-assignment fidelity:** the worker ran pwsh 7 despite the plan's explicit prohibition. Either (a) restate in the worker brief that the sandbox evidence lane is python-only and any extra verification goes in the PR body **labeled with its environment** ("pwsh 7 Linux — NOT the PS 5.1 tier"), or (b) amend the plan to sanction the pwsh 7 smoke run under that label, since it caught the attempt-1 parse error early. Prefer (b) — it's real signal; it just must never be presented as the host tier.
4. **Next attempt's brief (two one-line code items, host-verifiable):** replace `$MyInvocation.MyCommand.Path` with `$PSCommandPath` in `Install-ScheduledTask` (and note the same latent bug at `stack-watchdog.ps1:1573` as a separate issue — do not drive-by fix it in this PR); reword the CLEANUP-PLAN tick to "MM lane DONE 2026-08-23; OWUI lane = follow-up (OB1 submodule PR)" so the struck bullet doesn't overclaim.
5. **Host harness reminder:** on resubmission, the host session must run the logic tier (`-Mode check -SnapshotFile … -DryRun` ×2 + idle) under Windows PowerShell 5.1 and attach the transcript before this gate re-reviews — the gate session's own execution is approval-blocked, so the transcript must arrive with the PR.
