{
  "goal": "A subject handed to the collapse detector as a multi-word topic is reduced to the name inside it, so a healthy search for '100 Hz audio and VR motion sickness' is never reported as a search failure; the entity's length is bounded at extraction and checked at use.",
  "artifact": "Patch to OB1/integrations/research-service/search-quality.ts (entityCore for digit-first and long entities), harness.ts (KEYWORDIZE entity bounded to a NAME of at most 3 tokens, with a deterministic shortening + count when the model returns more), tests pinned on the recorded dry-run 6975d982 subject string and hit set; TEST-PLAN with the two live dry runs as the proof.",
  "audience": "The operator reading a deep-research report in Open WebUI, who must be able to trust 'search: DEGRADED' as a statement about the search plane.",
  "acceptance": [
    "entityCore('100Hz audio VR motion sickness') yields a core that the recorded hit set for '100Hz audio VR motion sickness physiological mechanism' (PMC11955832 at rank 1) satisfies at share >= 0.4; classifyHits returns ok. Today: core = all six tokens, share 0.00, collapsed.",
    "The rule for a digit-first or long entity is stated in one docblock sentence and pinned with tests for: '100Hz audio VR motion sickness', '2026 Toyota Prius', 'Python 3.12 asyncio', 'Dell OptiPlex 3050 used purchase', 'Kubernetes CrashLoopBackOff'; each with the expected core and a reasoned expected verdict on a realistic set.",
    "KEYWORDIZE's returned entity longer than 3 tokens is shortened deterministically to the name inside it, counted in fetchStats.search (entity_shortened) and shown in the footer; a test supplies a five-word entity.",
    "The six collapse fixtures still collapse; the good sets (oomkilled, iphone, good-optiplex, live 100hz-mechanism, live 100hz-studies, live optiplex-thermal, live optiplex-health) still classify ok; the ENTITY_SHARE table in the sink is extended with the recomputed shares and the no-set-within-0.1 rule still holds or the threshold is re-chosen with the table shown.",
    "After deploy, a live dry run of the 100 Hz query fetches pages and cites a URL matching pubmed.ncbi.nlm.nih.gov/40128952, pmc.ncbi.nlm.nih.gov/PMC11955832 or jstage.jst.go.jp; the OptiPlex dry run still cites at least 5 relevant sources with a consistent footer.",
    "deno test -A in research-service stays green with at least 173 passed."
  ],
  "out_of_scope": [
    "The weak-core class (laptop 5 / model 3) from findings E.10 unless the same rule fixes it for free.",
    "URL matching in entityShare (X3).",
    "Any change to the curator, engine policy, gateway, or report structure."
  ],
  "findings_sink": "documentation/notes/research-trust-findings.md"
}
