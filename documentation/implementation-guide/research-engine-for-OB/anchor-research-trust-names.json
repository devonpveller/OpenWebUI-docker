{
  "goal": "A name, standard, product or organisation that the grounded answer never uses cannot reach the delivered document unremarked: the fidelity check treats every unit containing such a name as UNSUPPORTED and corrects it, so the reporter that has flagged ATX/SFX, BSOD and OEM across three items finally has teeth; and a correction to a coarse multi-sentence unit replaces only the sentence that was wrong.",
  "artifact": "Patch to OB1/integrations/research-service/fidelity.ts and grounding.ts with tests: (1) renderGroundingDiff's names feed the fidelity check before judging - any unit whose view contains a flagged name is forced UNSUPPORTED (rewrite once, then verbatim fallback), with a documented exception for a name whose expansion appears in the synthesis (BSOD / Blue Screen of Death) resolved by a pure expansion match, not a list; (2) a coarse unit (two sentences sharing one span) that receives STRONGER/UNSUPPORTED is corrected per sentence: the judge is re-asked per sentence within the span and only the failing sentence is replaced, the sibling sentence kept byte-identical; (3) the footer gains 'names: K blocked' when any were; both renderers byte-identical.",
  "audience": "The operator handing a report to a colleague who will take a named standard or part at face value.",
  "acceptance": [
    "Re-rendering the recorded 64ac38cf buyers-guide fixture: the sentence 'the availability of an OEM replacement part is left open' is corrected to a form using only names the synthesis uses, and the fidelity record shows names blocked = 1; the attempt-1 ATX/SFX cell (rendered-AFTER-v1 fixture of the report item) is blocked the same way.",
    "A unit containing BSOD where the synthesis says 'Blue Screen of Death' is NOT blocked (expansion match), pinned by a test; a unit containing an unrelated acronym with no expansion in the synthesis IS blocked.",
    "The tester's X1 sample ('The PSU fails with a brief green LED. [Source 7] The connector is proprietary [Source 13].') with a judge that condemns only the second sentence keeps the first byte-identical and replaces only the second; pinned.",
    "The four INVARIANT tests still pass (byte-identity, idempotence, welding shapes, committed renders unchanged); deno test -A (service directory only) stays at or above 273 passed; renderers byte-identical on the new footer clause.",
    "After deploy, a live OWUI run's delivered document contains no name flagged by the grounding diff except expansion-matched abbreviations, and the footer states the blocked count."
  ],
  "out_of_scope": [
    "Templates, the gap-closing pass, the entity gate, the curator (all unchanged).",
    "Source weighting for which finding leads the title."
  ],
  "findings_sink": "documentation/notes/research-trust-findings.md"
}
